---
layout: post
title:  "Coreos Setup 2"
date:   2016-05-10 12:12:48
categories: sites coreos
tags: coreos
---

# Automatic Updates

1. To check the CoreOS version in the node

```
    cat /etc/os-release
```

2. To check the CoreOS Linus kernel version

```
    uname -a
```

3. To check versions of critical system components

```
    etcd2 --version
    fleet --version
    docker --version
    rkt version
```

5. To check partition table of /dev/sda

sudo cgpt show /dev/sda


4. To automatic updete

In user_data file, before units add

```
  update:
    reboot-strategy: "etcd-lock"
    group: "stable"
```

# Using ETCD2

Etcd is a distributed key-value store used by all the machines in the CoreOS clustor to read/write and exchange data.


# Using SYSTEMD


# Using FLEET


### nginx@.service file

```
[Unit]
Description=nginx

[Service]
ExecStartPre=-/usr/bin/docker kill nginx-%i
ExecStartPre=-/usr/bin/docker rm nginx-%i
ExecStart=/usr/bin/docker run --rm --name nginx-%i -p 80:80 nginx
ExecStop=/usr/bin/docker stop nginx-%i

[X-Fleet]
Conflicts=nginx@*.service
````

#### nginx-discovery@.service file

```
[Unit]
Description=Announce Nginx
BindsTo=nginx-@%i.service
After=nginx-@%i.service

[Service]
ExecStart=/bin/sh -c "while true; do etcdctl set /services/website/nginx-@%i '{ \"host\": \"%H\", \"port\": 80, \"version\": \"52c7248a14\" }' --ttl 60;sleep 45;done"
ExecStop=/usr/bin/etcdctl rm /services/website/nginx-@%i

[X-Fleet]
MachineOf=nginx@%i.service
```




# Using FLANNELD